name: .NET

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

permissions:
  contents: read
  packages: read

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      DOTNET_TEST_USE_MODERN_OUTPUT: true
    steps:
      - uses: actions/checkout@v6

      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: '11.0.x'
          dotnet-quality: 'preview'
          source-url: https://nuget.pkg.github.com/atulin/index.json
        env:
          NUGET_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Restore dependencies
        run: dotnet restore

      - name: Build
        run: dotnet build --no-restore

      - name: Test
        run: dotnet test -c Release -- --coverage --coverage-output-format cobertura --coverage-output coverage.cobertura.xml

      - name: Coveralls
        uses: coverallsapp/github-action@v2
        with:
          files: >
            Utils.Tests/bin/Release/net11.0/TestResults/coverage.cobertura.xml
            Utils.Tests/bin/Release/net11.0/TestResults/Utils.Tests-windows-net11.0-report.html
            Ogma3.Tests/bin/Release/net11.0/TestResults/coverage.cobertura.xml
            Ogma3.Tests/bin/Release/net11.0/TestResults/Ogma3.Tests-windows-net11.0-report.html
