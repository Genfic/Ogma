<Project>
	<PropertyGroup Condition="'$(PublishProtocol)' == 'Container' Or '$(ContainerImageName)' != ''">
		<CopyLocalLockFileAssemblies>true</CopyLocalLockFileAssemblies>
	</PropertyGroup>
</Project>