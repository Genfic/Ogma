<svg xmlns="http://www.w3.org/2000/svg" id="icon-spritesheet" style="display: none">
    <% svgs.forEach(({ svg, name }) => { %>
        <symbol id="<%= name %>" viewBox="0 0 24 24"><%- svg %></symbol>
    <% }) %>
</svg>