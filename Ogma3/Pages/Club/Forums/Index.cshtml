@page "/club/{id}/forums"
@model Ogma3.Pages.Club.Forums.IndexModel

@{
    ViewData["Title"] = $"{Model.ClubBar.Name} — Forums";
    ViewData["Tab"] = "Forums";
}

<partial name="_ClubBar" model="@Model.ClubBar"/>

@if (Model.ClubBar.IsMember)
{
    <a class="btn btn-block btn-outline-primary" asp-page="Create" asp-route-id="@Model.ClubBar.Id"> 
        <icon icon="add"></icon>
        Create new thread
    </a>
}

@foreach (var thread in Model.ThreadCards) {
    <partial name="_ThreadCard" model="thread"/>
}

