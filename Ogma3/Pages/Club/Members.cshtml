@page "/club/{id:long}/{slug}/members"
@model Ogma3.Pages.Club.Members

@{
    ViewData["Title"] = $"{Model.ClubBar.Name} — Members";
    ViewData["Tab"] = "Members";
}

<partial name="Shared/Bars/_ClubBar" model="@Model.ClubBar"/>

<ul class="two-columns o-grid">
    @foreach (var m in Model.ClubMembers)
    {
        <partial name="Shared/Cards/_UserCard" model="m"/>
    }
</ul>