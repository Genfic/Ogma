@page "/user/{name}/follows"
@model Ogma3.Pages.User.Follows

@{
    ViewData["Title"] = $"Users followed by {Model.Name}";
    ViewData["Tab"] = "Follows";
}

<vc:profile-bar name="@Model.Name" />

<h1>Users followed by @Model.Name</h1>

<hr>

<div class="two-columns o-grid">
    @foreach (var u in Model.Users)
    {
        <partial name="Shared/Cards/_UserCard" model="u"/>
    }
    
    <partial name="_Pagination" model="Model.Pagination"/>
</div>