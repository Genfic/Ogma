@page
@model StaffModel

@{
	ViewData["Title"] = "Staff";
	ViewData["Description"] = "A list of staff members";
}

<h1>Staff</h1>

<hr>

<div class="two-columns o-grid">
	@foreach (var s in Model.Staff)
	{
		<partial name="Shared/Cards/_UserCard" model="s"/>
	}
</div>