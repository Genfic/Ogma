@page "{id}/{slug?}"
@model TagModel
@{
    ViewData["Title"] = string.IsNullOrEmpty(Model.Tag.NamespaceName) 
        ? Model.Tag.Name
        : $"{Model.Tag.NamespaceName}:{Model.Tag.Name}";
}


<h2>@ViewData["Title"]</h2>

<div>
    @foreach (var story in Model.Stories)
    {
        <partial name="Shared/_StoryCard" model="@story"/>
    }
</div>
