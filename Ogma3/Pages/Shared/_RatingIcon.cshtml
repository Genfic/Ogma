@model RatingIcon

@{
	var icon = $"tabler:letter-{char.ToLower(Model.Name[0])}";
	var color = $"#{Model.Color}";
}

<div title="@Model.Name" class="rating-icon" style="--rating-color: @color">
	<icon name="@icon" ></icon>
</div>