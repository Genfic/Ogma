@model FolderMinimal

<div class="folder-minimal active-border">
    <a class="name" asp-page="/Club/Folders/Folder" asp-route-id="@Model.Id" asp-route-clubid="@Model.ClubId">
        @Model.Name
    </a>
    
    <div class="count">
        <strong>@Model.StoriesCount</strong> stories
    </div>
</div>

